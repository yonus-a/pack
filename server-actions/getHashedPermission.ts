"use server";


export default function getHashedPermission(permission: number) {
  const adminKey = process.env.ADMIN_KEY;
  const userKey = process.env.ADMIN_KEY;

  switch (permission) {
    case 1: return 
    case 2:
  }
}
