"use server";

import prisma from "@/lib/prisma";

export default async function getProductLimitBudget() {
  try {
  } catch (e) {
    console.error(e);
    throw new Error("مشکلی در سرور به وجود آمده است !");
  }
}
