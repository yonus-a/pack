generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model user {
  id         String @id
  firstname  String
  lastname   String
  phone      String
  permission String
}

model otp {
  id  String @id
  otp String
}
